# Family Tree 视图规范 v1

KRIG Graph 的"族谱"变种 — 专门用于人物亲属关系图。

## 1. 设计原则

1. **对标经典专业族谱工具**。family-tree variant 的目标是 GenoPro / GRAMPS / MyHeritage / Reunion 等专业工具的水准 — 能扛各种家族数据(欧洲贵族多代联姻、东方宗族继承、近现代多次婚姻、领养收养、私生子、占位人物等)。**红楼梦只是首批验证用例之一**,后续会用其他族谱(如欧洲皇室、近代历史人物)继续验证。
2. **专属变种,不通用化**。family-tree 是独立、完整的图类型,**不**复用通用层级树的布局/边路由。互通问题(其他 ViewMode 切换为 family-tree)留待 v2 设计。
3. **GEDCOM 兼容数据模型**。即使 v1 也按 GEDCOM 标准建模(Individual + Family + ChildLink 三类),为后续导入导出 .ged 留出空间;数据模型从 day 1 就支持单亲 / 多次婚姻 / 占位 / 不同子女关系类型等真实族谱场景。
4. **照搬专业族谱视觉惯例**。所有视觉决策来自 GenoPro / GRAMPS / 专业族谱学界共识,不自创规则。
5. **v1 范围 = 通用核心能力**。包含多次婚姻 / 同时多妻 / 单亲 / 占位 / rel_type 区分 / 嫡庶视觉等专业必备;祖先视图 / 沙漏 / GEDCOM 文件互通等留 v1.5+(详见 §8)。
6. **还原历史真实,不抹平文化差异**。族谱是历史 / 家族真实记录工具,不是社会工具。视觉默认区分嫡庶 / 正侧 / 虚线表达私生 / 收养等真实关系——用户填数据怎样,视觉就忠实呈现怎样。中国古代嫡庶有别就画出嫡庶,伊斯兰四妻平等就画平等(用户全填 `principal`),欧洲贵族继承嫡长就突出嫡长。**数据中性命名 + 视觉如实呈现**是这个原则的工程落地。

## 2. 数据模型

### 2.1 Person(节点)

`Point` geometry + intension atom:

| predicate | value | 说明 |
|---|---|---|
| `substance` | `family/person` | 固定 |
| `label` | string | 姓名(显示用) |
| `gender` | `'M'` \| `'F'` \| `'O'` \| `'U'` | M=男 F=女 O=其他(包含中性 / 不公开) U=未知 |
| `birth_date` | `'YYYY'` 或 `'YYYY-MM-DD'` | 出生年(可省) |
| `death_date` | `'YYYY'` 或 `'YYYY-MM-DD'` | 死亡年(在世则不写) |
| `placeholder` | `'true'` | 占位人物(配偶 / 父母身份已知但具体信息全无),只画虚线占位框 |

**alive 自动派生**:`death_date` 不存在 → alive。

**placeholder 用途**:
- 历史族谱中常见"知道有这个人但不知道是谁"的情况(贾宝玉的曾祖父辈、林黛玉外祖母辈等)
- 或某 Family 的配偶仅知 1 人,另一个为占位
- 视觉:虚线圆角矩形 + 文字 `?` 或 `(未知)` + 灰色

GenoPro 文档参考:["unknown person placeholder"](https://genopro.com/genogram/rules/) 的标准用法。

### 2.2 Family(婚姻 / 家庭单元)

GEDCOM `FAM` 等价物。**一对配偶 + 他们的共同子女** = 一个 Family。

`Point` geometry + intension atom:

| predicate | value | 说明 |
|---|---|---|
| `substance` | `family/family` | 固定 |
| `partner` | `[[person-id]]` | 配偶引用,可出现 0 / 1 / 2 次 |
| `marriage_date` | `'YYYY'` | 结婚年(可省) |
| `marriage_status` | `'married'` \| `'unmarried'` \| `'civil_union'` \| `'divorced'` \| `'separated'` \| `'unknown'` | 详见下表 |
| `marriage_order` | `1` / `2` / `3` ... | 该 Family 在配偶人生中是第几次婚姻(可省,默认按 marriage_date 排序;用于顺序多次婚姻视觉左右排序) |
| `spouse_rank` | `'principal'` \| `'secondary'` \| `'unknown'` | **配偶在该 Family 中的层级**(见下文)。默认 `'principal'`。文化中性命名:中国古代嫡=principal、妾=secondary;伊斯兰四妻全 principal;欧洲贵族正室=principal、情妇=secondary。 |
| `concurrent_with` | `[[family-id-1]] [[family-id-2]] ...` | **与之并存的其他 Family**。用于区分"同时多妻"和"再婚"。默认空(顺序婚姻)。 |

**partner atom 数量约束**:
- **2 个 partner**:正常双配偶家庭(同性 / 异性都行,数据模型不区分)
- **1 个 partner**:单亲家庭(寡妇 / 私生子的母亲 / 单身收养等)
- **0 个 partner**:**仅作占位**,用于"已知有这么个家庭单元但配偶信息全部缺失"的极端情况(罕见,通常用 1 partner 即可)

**marriage_status 视觉**:

| 值 | 婚姻横线视觉 |
|---|---|
| `married`(默认) | 实线 |
| `civil_union` | 实线(同 married,无视觉差异) |
| `unmarried` | 虚线(同居 / 未正式结婚) |
| `divorced` | 实线 + 中点画双斜线 `//` |
| `separated` | 实线 + 中点画单斜线 `/` |
| `unknown` | 实线 + 半透明 50% |

**多个配偶的两种语义(关键区分)**:

一人参与多个 Family,数据上都是 "多个 Family 共用同一 partner"。但**语义截然不同**,视觉也不同:

#### A. 顺序多次婚姻(serial marriage)

- 离婚 / 丧偶后再娶,**同一时刻只有一位配偶**
- 多个 Family **互不 concurrent_with**(`concurrent_with` 空 / 不填)
- 例:亨利八世 6 次婚姻、近代离婚再婚
- **视觉**:该人居中,所有配偶一行排开,按 `marriage_order` 排序 — 第 1 任最近(GenoPro 规则:"first spouse closest")
- 离异/丧偶配偶用 `marriage_status='divorced'` / 死亡日期表达,无需特殊布局区分

#### B. 同时多妻 / 多夫(concurrent polygamy)

- 多个配偶**同时存在**,无先后
- 多个 Family **互相 concurrent_with**(双向链接)
- 例:中国古代正妻 + 妾、伊斯兰一夫四妻、藏族传统一妻多夫、历史摩门教
- **视觉**:该人居中,所有 concurrent 配偶一行排开,按 `spouse_rank` + `marriage_order` 排序 — `principal` 最近(嫡正在内),`secondary` 外扩(妾室在外)

#### 混合情况(合理,要支持)

- 一人**先后娶过多个正妻**(各自时期为 principal),例如续弦正妻
- 一人**同时拥有正妻 + 数个妾**(中国皇室常见,正妻一直在,妾室来去)
- 数据模型自然支持:每段 Family 自己声明 `spouse_rank` + `concurrent_with`,布局算法读取后做判断

**渲染**:Family 节点本身**不可见**(`visual.shape = 'invisible'`),只作为布局锚点 + 婚姻横线的中点。婚姻横线由 projection 在两个 partner 之间画(单 partner 时不画,只在 partner 下方留 drop-line 起点)。详细视觉规则见 §3.3。

### 2.3 ChildLink(亲子边)

`Line` geometry + intension atom:

| predicate | value | 说明 |
|---|---|---|
| `substance` | `family/child-link` | 固定 |
| `members[0]` | `family-id` | source = Family(婚姻锚点) |
| `members[1]` | `person-id` | target = 子女 Person |
| `rel_type` | `'birth'` \| `'adopted'` \| `'foster'` \| `'step'` \| `'unknown'` | 默认 `birth` |

**关键**:子女连的是 **Family 节点**,不是某个 Parent。这是 GEDCOM 模型,确保多子女共享同一条 drop-line。

### 2.4 Markdown frontmatter

```yaml
---
title: 红楼梦人物族谱
graph_variant: family-tree
---
```

`graph_variant: family-tree` 必须存在。导入时校验,未声明则报错(family-tree 数据进非 family-tree 图谱视觉会乱)。

### 2.5 Markdown 节点表达

```markdown
# 贾宝玉 [[jia-baoyu]]

- substance :: family/person
- gender :: M
- birth_date :: 1716

# 贾政 & 王夫人 [[fam-zhengwang]]

- substance :: family/family
- partner :: [[jia-zheng]]
- partner :: [[wang-furen]]
- marriage_date :: 1700

# 婚姻产子 [[child-link-baoyu]]

- substance :: family/child-link
- members :: [[fam-zhengwang]] [[jia-baoyu]]
- rel_type :: birth
```

(具体 markdown parser 实现细节由现有 import/parser.ts 扩展,本 spec 不重复)

## 3. 视觉规范

照搬专业族谱工具的通用约定(GenoPro / GRAMPS 共识)。

### 3.1 节点视觉(Person)

#### 标准 Person(主妻所生 / 默认)

- **形状**:圆角矩形 **160 × 60 px**(`borderRadius=6`)
- **颜色填充**:
  - M(男):`#a8c7e8`(浅蓝)
  - F(女):`#e8a8c0`(浅粉)
  - O(其他):`#c0c0c0`(浅灰)
  - U(未知):`#888`(深灰)
  - 已故:对应颜色饱和度降到 50%
- **边框**:1px **实线**,深一档同色系
- **文字**:
  - 第 1 行:姓名(label),14px,`#000`
  - 第 2 行:`b. 1716 – d. 1763`(无生卒省略),11px,`#444`
- **死亡标记**(GRAMPS 同款):左上角对角斜线,从 `(0, 10)` 到 `(10, 0)`,1px 黑

#### 庶出 Person(母亲是 secondary 配偶)

如何识别:从 ChildLink 上溯到 Family,Family 中该 Person 的母亲(F 性别 partner)对应的 Family 节点 `spouse_rank=secondary`。

视觉差异(还原历史地位差异,但不夸张):

- **尺寸略小**:**140 × 50 px**(略小于嫡子 160 × 60)
- **边框**:1px **虚线**(`dashSize=4, gapSize=2`),颜色同色系深一档
- 颜色填充、文字、死亡标记同标准 Person

意图:用户一眼能区分嫡庶,但不刻意贬低或灰化。这是中国古代族谱、欧洲贵族家谱里嫡长 vs 私生 / 旁支的标准呈现方式。

#### 配偶 Person(在 Family 中的视觉差异)

如果 Person 作为某 Family 的 partner 出现且该 Family 中其 `spouse_rank=secondary`(即该人是妾 / 侧室):
- **节点尺寸缩到 140 × 50**(同庶出)
- **边框 1px 实线**(实线 — 表示该人本身存在确实,非占位虚线)
- 排序在主妻外侧(详见 §3.3 多妻视觉)

#### 占位 Person(`placeholder=true`)

- 边框改为**虚线**(`dashSize=4, gapSize=2`)
- 内文显示 `?`(20px 灰色 `#888`),不画 b./d. 行
- 填充 `#3a3a3a`(深一档,提示"未实")
- 不画死亡斜线
- 尺寸 140 × 50(同庶出 / 妾,视觉次要)

**注**:庶出 / 妾(实存但地位次)和占位(身份未知)用**虚线 vs 实线**做主要区分,尺寸都是 140 × 50;两者不会混淆。

### 3.2 节点视觉(Family)

- **不渲染 mesh**(`shape = 'invisible'`)
- 只作为布局锚点和婚姻横线的几何中点

### 3.3 边视觉

#### 婚姻线(双 partner Family)

- 由 family-tree projection 自己生成,**不**走 ChildLink
- 算法:对每个 Family,在两个 partner 节点之间画一条**水平线**(在两人 y 平均值的高度上,通常两人 y 相同)
- **线宽根据 `spouse_rank`**:
  - `principal`(默认 / 主妻 / 正室):**1.5px**(标准)
  - `secondary`(妾 / 侧室):**1px**(细线,视觉次要)
  - `unknown`:1.5px(同 principal)
- 色 `#666`
- 横线**穿过 Family 锚点**(锚点在两人中点)
- 根据 `marriage_status` 决定线型(详见 §2.2 表):
  - `married` / `civil_union`:实线
  - `unmarried`:虚线 4-2
  - `divorced`:实线 + 中点画 `//`(双斜线,长度各 8px,间隔 3px)
  - `separated`:实线 + 中点画 `/`(单斜线,长度 8px)
  - `unknown`:实线半透明 50%

#### 婚姻线(单 partner Family)

- 不画横线
- Family 锚点直接位于 partner 节点正下方(`partner.x, partner.y - boxHeight/2 - layerGap/4`)
- drop-line 起点 = Family 锚点,延伸到子女 sibling bar(语义:"该 partner 的孩子,另一方未知")

#### 亲子边(ChildLink)

drop + sibling bar + stub 三段直角:

```
         family-anchor
              |        ← drop line(垂直,长度 = layerGap / 2)
              |
       ┌──────┴──────┐ ← sibling bar(水平,跨所有兄弟 x)
       |             |
     [child]       [child]   ← stub(短垂直,从 sibling bar 到子节点顶部)
```

- 线宽:1.5px
- 色:`#666`(BIRTH)
- **rel_type 不是 BIRTH**:9px 虚线(`dashSize: 9, gapSize: 4`),其中:
  - `birth`(默认):实线
  - `adopted`:虚线
  - `foster`:点线(`dashSize: 2, gapSize: 3`)
  - `step`:虚线(同 adopted)
  - `unknown`:虚线
- **每条 ChildLink 独立 rel_type**:同 Family 不同子女可以是不同关系(birth + adopted 共存),sibling bar 仍共享,但每条 stub 自己的线型独立绘制
- 多个兄弟共享同一条 drop-line + sibling bar(关键:projection 在 §5.1 实现这个共享)

#### 顺序多次婚姻视觉(serial,concurrent_with 空)

一人参与多个 Family,以 Person A 为中心展示:

```
[ex-spouse]──[A]──[current-spouse]
   ╲          │            ╱
   ╲     (drop)            ╱
[children of            [children of
 1st marriage]           2nd marriage]
```

- A 居中
- 第 1 次婚姻配偶在 A 的最近一侧(GenoPro 规则:"first spouse closest"),按 `marriage_order` 排序
- 第 2 次次之,以此类推向外扩展
- 离异配偶用 `marriage_status='divorced'` 表达(线上双斜线),布局位置同正常配偶
- 每个 Family 独立 drop-line + sibling bar
- 所有配偶 + A 同 y(同代行)

#### 同时多妻 / 多夫视觉(concurrent,concurrent_with 互链)

中国古代正妻 + 妾、伊斯兰一夫多妻、藏族一妻多夫等场景,以一夫多妻为例(Person A 为夫):

```
            [A 夫]
       ╱      ║      ╲       ╲
   [正妻]══[A]──[妾 1]────[妾 2]
   ┃        ┃        ┃            ┃
 (drop)  (drop)   (drop)       (drop)
   ┃        ┃        ┃            ┃
 [嫡子] [嫡女]   [庶子]         [庶子]
```

- A 居中
- 所有 concurrent 配偶按 `marriage_order` 升序向同一方向扩展(布局算法不读 spouse_rank)
- 每个 Family 独立 drop-line + sibling bar
- 所有配偶 + A 同 y
- **视觉差异由节点 / 婚姻线视觉自动呈现**(算法负责"摆位置",视觉负责"还原文化"):
  - 节点:principal 配偶用标准尺寸 + 实线;secondary 配偶用 140×50 + 实线略小(§3.1)
  - 子女:母亲 spouse_rank=principal → 嫡子嫡女标准视觉;母亲 spouse_rank=secondary → 庶子庶女 140×50 + 虚线视觉(§3.1)
  - 婚姻线:principal 1.5px 粗,secondary 1px 细(§3.3)
  - 看红楼梦贾政:王夫人(principal)+ 赵姨娘(secondary)同代并排 → 视觉自动呈现"嫡庶有别"

#### 一妻多夫(polyandry,数据模型同上)

数据模型完全对称(只是性别对调),布局算法和视觉规则一视同仁 —— 只看 spouse_rank 决定视觉,不区分父系母系。

#### 混合(顺序 + 并存)

例:某人前妻已故 → 续弦正妻 + 同期纳妾。
- 前正妻(`marriage_order=1`,无 `concurrent_with`,有 `death_date`)
- 续正妻(`marriage_order=2`,`concurrent_with=[妾 family]`,`spouse_rank=principal`)
- 妾(`marriage_order=2`,`concurrent_with=[续正妻 family]`,`spouse_rank=secondary`)

布局算法仅按 `marriage_order` 排序(前正妻在最远端,续正妻次之,妾最近 — 因为 marriage_order=1/2/2);视觉由节点/婚姻线自动呈现:前正妻有死亡斜线,续正妻和妾分别按 spouse_rank 显示尺寸/线粗细差异。

### 3.4 配色 token

写在 substance built-in/family.ts 内,允许用户后续主题化覆盖。

## 4. 布局算法

### 4.1 总览

新建 `src/plugins/graph/layout/family-tree.ts`,**不接 ELK**(ELK 处理不了 couple 概念)。

参考 [entitree-flex](https://github.com/codeledge/entitree-flex) 思想:Walker 的 tidy tree 算法 + couple-as-side-node 扩展。

### 4.2 输入识别

- **Person 节点**:`substance = family/person`
- **Family 节点**:`substance = family/family`(读 `partner` atom,可能 0/1/2 个)
- **ChildLink 边**:`substance = family/child-link`(读 `members` 第一个是 family,第二个是 child)

### 4.3 算法

#### 第 1 步:建索引

```
partnerOf(person)  = 该 person 作为配偶参与的所有 family ids(可能多个 — 多次婚姻 / 多妻)
spousesOf(family)  = [partner-1, partner-2]  (0/1/2 个)
familyAsChild(person) = 该 person 作为子女所在的 family(BFS 向上找父母)
childrenOf(family) = [child-1, child-2, ...]
rankOf(family)     = family 的 spouse_rank ('principal' / 'secondary' / 'unknown')
concurrentOf(family) = 该 family concurrent_with 引用的其他 family ids
isSerialFamily(family) = concurrentOf(family) 为空 → 顺序婚姻
isConcurrentFamily(family) = concurrentOf(family) 非空 → 同时多妻
```

#### 第 2 步:确定主人公(proband)+ 代际分配

- 主人公 = graph 第一个 Person 节点(v1 简化;v1.5 加"设为主人公"按钮)
- 主人公 generation = 0
- 从主人公 BFS 向下(只走 family→child 方向):
  - 主人公的所有配偶 generation = 0(同代)
  - 主人公 / 主人公配偶的 children → generation = 1
  - 以此类推
- 主人公 BFS 向上(只走 child→family→partner 方向,v1.5 加;v1 假设主人公就是最年长一代,不画祖先)

#### 第 3 步:同代 x 排布(Walker tidy tree 变种 + 多配偶扩展)

**核心扩展**:Walker tidy tree 原版只处理"严格树",族谱里一人多个 Family = 该 person 是多个 Family 的 partner,不是树形。处理方法:

1. **Person primary family**:对每个 person,选一个 "primary family"(配偶身份所在的 Family)作为该 person 在 layout 中的"主位置归属"
   - **优先级**:`marriage_order=1` > `marriage_date` 最早 > 在 markdown 中最早出现
   - **不读 spouse_rank** — primary family 是布局概念(决定 layout 主位置),与文化语义解耦

2. **Secondary spouse cluster**:其他 Family(非 primary)的另一方 partner 作为 person 的"侧 cluster"

3. **底层叶子优先**:按代从底向上算
   - 叶子(无 children family)按 `birth_date` 升序排
   - 缺失 birth_date 时按 markdown 出现顺序
   - **算法不读 spouse_rank** — 嫡庶是文化语义,不影响布局排序。视觉差异由 §3.1 节点视觉自动呈现;同代兄弟按出生日期自然混排(嫡庶可能交错,符合历史真实:贾环出生在贾宝玉之后但在贾探春之前,自然就在中间)

4. **父代位置由子代决定**:每个 Family 的 x = `(childrenOf(family).leftmost.x + childrenOf(family).rightmost.x) / 2`

5. **配偶相对位置**(Family 的两个 partner):
   - 双 partner:左 partner.x = `family.x - (partner.width + coupleGap) / 2`,右 partner.x = `family.x + (partner.width + coupleGap) / 2`,同 y
   - 单 partner:partner.x = family.x(正上方),y 同代
   - 0 partner:不绘制(罕见,跳过)

6. **多配偶簇排布**(person A 参与多个 Family):

   **6a. 顺序多次婚姻(`isSerialFamily=true`,所有 Family 互不 concurrent)**
   - A 的最终 x = primary family 决定的 x
   - 第 1 次婚姻配偶在 A 最近一侧(GenoPro 规则)
   - 第 N 次婚姻配偶在第 N 位向外扩
   - 简单规则:从 A 出发向右,按 `marriage_order` 升序依次放置每个 secondary spouse

   **6b. 同时多妻 / 多夫(`isConcurrentFamily=true`,Family 之间互链 concurrent_with)**
   - A 的最终 x = primary family(`principal` 那个 Family)决定
   - **`principal` 配偶在 A 紧邻一侧**(主妻紧贴),传统中国族谱惯例:主妻在左 / 西
   - **其他配偶按 `marriage_order` 升序向外扩**
   - 每个 Family 各自的 children 簇跟随其 family 锚点向下展开
   - **算法只用 `marriage_order` 决定排序,不读 spouse_rank**;视觉差异由 §3.1 节点视觉 + §3.3 婚姻线粗细自动呈现 — 主妻和妾在 layout 上同等对待,但视觉上一眼可分

   **6c. 混合(serial + concurrent)**
   - 例:前正妻已故 + 续正妻 + 同期纳妾
   - 算法:先按 isConcurrentFamily 分组,每组按 6b 处理(选出该组 primary,其他向外扩)
   - 不同组之间按 marriage_order(已故组在最远侧)
   - 实现细节较多,v1 实施时按测试数据驱动调试

#### 第 4 步:y 坐标

- 节点 y = `-generation * layerGap`(KRIG y-up,代数越大 y 越小)
- Family 锚点 y = `(spouse-1.y + spouse-2.y) / 2`(双 partner)或 `spouse.y`(单 partner)
- `layerGap = 120`(默认,可由 `layout.spacing.layer` 覆盖)

#### 第 5 步:碰撞处理

- 多次婚姻 + 兄弟簇横向占地大时,要保证同代节点不重叠
- Walker 算法的 `apportion` 阶段处理 subtree 间隙 — v1 直接抄 entitree-flex 实现思路
- 如果同代节点超过画布宽度:不做缩放,允许超出(canvas 提供 pan/zoom)

#### 第 6 步:输出

- `LayoutOutput.positions`:每个 Person + 每个 Family 锚点的中心
- `LayoutOutput.edgeSections`:**留空**(由 family-tree projection 在渲染阶段生成)

### 4.4 默认参数

| 参数 | 默认值 | 用户可调? |
|---|---|---|
| `layout.spacing.sibling` | 30 | ✅ 兄弟之间间隔 |
| `layout.spacing.couple` | 10 | ✅ 配偶之间间隔(留出婚姻横线) |
| `layout.spacing.layer` | 120 | ✅ 代际间距 |
| `layout.spacing.spouse-cluster` | 40 | ✅ 多次婚姻 secondary 配偶与主配偶之间额外间距 |

(direction / edge-style 不消费 — 族谱永远是"上代在上、下代在下"的固定方向,边样式永远是"婚姻横+亲子直角")

## 5. Projection

新建 `src/plugins/graph/projection/built-in/family-tree.ts`。

### 5.1 customizeLine(ChildLink 渲染)

```
对每条 ChildLink line geometry:
  family = members[0] 的 Family 节点位置
  child  = members[1] 的 Person 节点位置
  生成 drop + sibling-bar + stub 三段路径
  (sibling-bar 跨所有同 Family 兄弟,因此 projection 需先扫一遍同 family 的兄弟集)
```

详细实现:由于多个 ChildLink 共享同一条 sibling bar,projection 在第一次扫描时**预计算每个 family 的兄弟 x 范围**,然后每条 ChildLink 都用这个共享的 bar。

### 5.2 customizeNode(Family 隐藏 + 婚姻线绘制)

projection 接口需要扩展:除了 `customizeLine`,加 `customizeScene`(每帧前额外渲染装饰性几何)。

或者更简单:**Family 节点用一个特殊的 LineSegmentShape 子类作为视觉**,不渲染 Family 的 mesh,而是把婚姻横线作为它的 mesh 输出。

具体方案 v1 实现时再定,本 spec 不锁。

### 5.3 节点视觉

不在 projection 做 — 由 substance `family/person` 的 visual 字段直接驱动现有 RoundedRect shape:

- `family/person` substance 注册时声明 `visual.shape = 'rounded-rect'`
- gender 颜色通过 visual override 写入(导入时按 gender intension atom 决定 fill.color)

死亡斜线:扩展 RoundedRectShape,看 visual 里有没有 `deceased = true` 标志,有则画对角斜线。

## 6. ViewMode 注册

```ts
viewModeRegistry.register({
  id: 'family-tree',
  label: '族谱',
  layout: 'family-tree',
  projection: 'family-tree',
  enable_patterns: false,
});
```

## 7. 入口集成

### 7.1 NavSide "+ 图谱" 变种选择

`useGraphOperations.ts` 的 `AVAILABLE_VARIANTS` 新增:

```ts
{ id: 'family-tree', label: '族谱', icon: '👨‍👩‍👧' },
```

`VARIANT_ICONS` map 同步加入。

### 7.2 GraphVariant 类型

`src/main/storage/types.ts` 的 `GraphVariant` union 加 `'family-tree'`。

### 7.3 创建图谱时自动设 ViewMode

新建 family-tree 图谱时,自动设 `active_view_mode = 'family-tree'`(graphViewStore.create 加逻辑根据 variant 推默认 view_mode)。

### 7.4 markdown frontmatter 校验

import/parser.ts 扩展校验 allowlist,`graph_variant` 必须在合法值集内,否则导入失败并提示。

## 8. 版本范围

### v1 必备(本次实施)

涵盖通用专业族谱工具的核心能力,确保 KRIG family-tree 不"只会画严格双亲单子"的玩具用例。

| 功能 | 备注 |
|---|---|
| 后代视图(Descendant tree,主人公向下) | 默认且唯一 v1 模式 |
| 双 partner Family(异性 / 同性,数据模型不区分) | 标准用法 |
| **单 partner Family**(寡妇带子 / 私生子等) | 历史族谱常见 |
| **顺序多次婚姻**(一人多个 Family,无 concurrent_with) | GenoPro 规则,marriage_order 排序 |
| **同时多妻 / 多夫**(concurrent_with 互链 + spouse_rank) | 中国古代正妻 + 妾;伊斯兰一夫四妻;藏族一妻多夫 |
| **嫡庶视觉区分**(母亲 spouse_rank 决定子女节点视觉) | 嫡子 160×60 实线,庶子 140×50 虚线 |
| **正妾视觉区分**(spouse_rank 决定配偶节点和婚姻线视觉) | 主妻 1.5px 婚姻线 + 标准尺寸,妾 1px + 略小 |
| **占位 Person**(`placeholder=true`,虚线框 + `?`) | 配偶 / 父母身份已知但具体信息全缺 |
| 子女 rel_type 区分(birth / adopted / foster / step / unknown) | 实线 vs 虚线 |
| marriage_status 视觉(married / civil_union / unmarried / divorced / separated / unknown) | 实线 / 虚线 / 斜线标记 |
| 已故标记(死亡日期 + 左上角斜线 + 颜色降饱和度) | GRAMPS 同款 |
| 性别色填充(M 蓝 / F 粉 / O 灰 / U 深灰) | |
| markdown 导入(frontmatter 校验 graph_variant) | |
| NavSide "+ 图谱" 变种选择列出族谱 | |

### v1.5(后续按需,影响小)

| 功能 | 备注 |
|---|---|
| 祖先视图(Ancestor pedigree,主人公向上) | 2^n Ahnentafel 网格,布局算法与 descendant 不同 |
| 点击换主人公(reroot) | 交互细节 |
| 添加家庭成员 UI(点空位弹编辑框) | 交互细节 |
| 双胞胎 / 三胞胎符号(sibling bar 上的 V 形) | 视觉细节 |
| 照片缩略图 | 节点视觉扩展 |
| GEDCOM 导入 / 导出(.ged) | 文件格式互通 |
| 兄弟代序自动按 birth_date 排(目前简化为出现顺序) | 排序细节 |

### v2(架构性扩展)

| 功能 | 备注 |
|---|---|
| 沙漏视图(Hourglass:祖先 + 后代) | descendant + ancestor 拼合 |
| 扇形图(Fan chart) | 极坐标变换 |
| 时间轴模式(y = birth_year) | 与代际行模式互斥 |
| 与其他 ViewMode 互通(family-tree → knowledge graph 数据共享) | 数据模型映射 |

## 9. 测试数据

### 9.1 红楼梦核心(首批验证 — 含嫡庶)

`docs/graph/samples/Family-Tree-Hongloumeng.md` — 红楼梦核心人物族谱,4 代约 20 人,**含嫡庶之分**。

- **第 0 代**:贾代善(已故)& 贾母(史太君,principal)
- **第 1 代**:贾赦 & 邢夫人;贾政 & 王夫人(principal)+ 赵姨娘(secondary);贾敏(已故)& 林如海(已故)
- **第 2 代**:贾琏 & 王熙凤;贾迎春;贾元春(嫡);**贾珠(已故,嫡)& 李纨**;贾宝玉(嫡);贾探春(嫡);**贾环(庶,赵姨娘所生)**;林黛玉
- **第 3 代**:贾兰(贾珠之子);巧姐(贾琏之女)

**关键嫡庶设计**:
- 贾政有 2 个 Family:`贾政&王夫人`(spouse_rank=principal)+ `贾政&赵姨娘`(spouse_rank=secondary,concurrent_with=[贾政&王夫人])
- 王夫人所生(元春 / 贾珠 / 宝玉 / 探春)= 嫡 → 标准节点视觉
- 赵姨娘所生(贾环)= 庶 → 140×50 + 虚线节点视觉

**v1 必验场景**:
- ✅ 多子女(贾政→元春/珠/宝玉/探春/环)
- ✅ 已故(贾代善 / 贾敏 / 林如海 / 贾珠)
- ✅ 多代深度(4 代)
- ✅ 跨家族婚姻(林如海&贾敏 → 林黛玉,跨家与贾府连)
- ✅ **同时多妻**(贾政 + 王夫人 + 赵姨娘 concurrent)
- ✅ **嫡庶视觉差异**(贾环明显与贾宝玉视觉不同)
- ✅ **婚姻线粗细差异**(王夫人 1.5px vs 赵姨娘 1px)

### 9.2 顺序多次婚姻验证(补充用例)

`docs/graph/samples/Family-Tree-SerialMarriage.md` — 选取现实历史案例:**亨利八世的六次婚姻**(英国都铎王朝)。

- 凯瑟琳(西班牙)→ 离婚 → 安妮·博林 → 处死 → 简·西摩 → 难产去世 → 安妮(克莱沃)→ 离婚 → 凯瑟琳·霍华德 → 处死 → 凯瑟琳·帕尔
- 子女:玛丽一世(凯瑟琳所生)、伊丽莎白一世(安妮·博林所生)、爱德华六世(简·西摩所生)

**v1 必验场景**:
- ✅ 一人 6 次婚姻(`marriage_order` 1-6)
- ✅ 离婚:`marriage_status='divorced'`(婚姻线双斜线)
- ✅ 多个不同 Family 各有子女(嫡子继承王位)
- ✅ 顺序婚姻:**所有 Family 互不 concurrent**,视觉左右排序

### 9.3 同时多妻验证(补充用例)

`docs/graph/samples/Family-Tree-Polygamy.md` — 中国古代典型案例:**康熙皇帝的后宫**(简化版,选 4-5 后妃 + 部分皇子)。

- 康熙(玄烨)& 孝诚仁皇后(principal)+ 孝昭仁皇后(principal,先后正妻,前者已故续娶)+ 德妃乌雅氏(secondary)+ 良妃卫氏(secondary)
- 皇子:胤礽(嫡,孝诚仁皇后所生)、胤禛(雍正,德妃所生,庶但后来登基)、胤禩(良妃所生,庶)

**v1 必验场景**:
- ✅ **同时多妻**(`concurrent_with` 互链)
- ✅ **混合**:前正妻已故 + 续正妻 + 同期纳妃(serial + concurrent 混合)
- ✅ **嫡庶并存**:嫡子胤礽 + 庶子胤禛 + 庶子胤禩 同代视觉差异
- ✅ **多个 secondary**(多妃同时存在)

### 9.4 占位 + 单亲(补充用例)

`docs/graph/samples/Family-Tree-Placeholders.md` — 测试 placeholder 和单 partner Family。

**v1 必验场景**:
- ✅ 占位父亲(只知母亲,父亲为虚线占位框)
- ✅ 占位整对父母(只知"是某祖先的孙辈",父母均占位)
- ✅ 单亲家庭(寡母带子,Family 只 1 个 partner)
- ✅ 私生子(rel_type=unknown,虚线 ChildLink)

## 10. 实施阶段

| 阶段 | 内容 | 预计 |
|---|---|---|
| 0 | 本 spec | ✅ |
| 1 | 数据模型:`family/person` / `family/family` / `family/child-link` substance 注册 + 4 套 markdown 测试数据(红楼梦含嫡庶 + 亨利八世 + 康熙后宫 + 占位) | 0.5 天 |
| 2a | family-tree layout 基础算法(双 partner / 单子女树 / 多代分层) | 0.5 天 |
| 2b | family-tree layout 扩展(单 partner / 占位 / 顺序多次婚姻) | 0.5 天 |
| 2c | family-tree layout 同时多妻(concurrent_with + 多个 Family 共享同一 person) | 0.5 天 |
| 3a | family-tree projection 基础(婚姻横线 + drop+bar+stub 亲子折线) | 0.5 天 |
| 3b | 视觉细节(性别色 / 死亡斜线 / rel_type 虚线 / marriage_status 斜线 / 占位虚线框 / 嫡庶尺寸差异) | 0.5 天 |
| 4 | ViewMode 注册 + NavSide 入口 + GraphVariant 类型扩展 + frontmatter 校验 | 0.5 天 |
| 5 | 4 套测试数据验证 + 调优 | 1 天 |

合计 5 天(含通用专业能力 + 同时多妻 + 嫡庶视觉)。

**每阶段交付暂停点**:
- 阶段 2a 算法跑通 → console 打位置 + 静态截图
- 阶段 2b 单亲/顺序多婚 → 跑亨利八世测试数据
- 阶段 2c 同时多妻(concurrent layout) → 跑红楼梦贾政/王夫人/赵姨娘(嫡庶视觉自动从节点视觉呈现,不需算法处理)
- 阶段 3a 婚姻线 + 亲子线 → 红楼梦核心(简化视觉)
- 阶段 3b 完整视觉(嫡庶 + 占位) → 完整红楼梦
- 阶段 5 → 4 套测试数据全部 pass

## 11. 验收标准

### 红楼梦核心(`Family-Tree-Hongloumeng.md`)

- [ ] markdown 导入成功,自动设为 family-tree variant 图谱 + family-tree ViewMode
- [ ] 视觉:
  - [ ] 4 代分层清晰(贾代善 → 贾政 → 贾宝玉 → 贾兰)
  - [ ] 配偶横线正确连接(贾政—王夫人 / 贾赦—邢夫人 / 林如海—贾敏 ...)
  - [ ] 多子女共享 drop-line + sibling bar(贾政→元春/珠/宝玉/探春/环)
  - [ ] 跨家族婚姻不出错(林如海家与贾家通过贾敏连接)
  - [ ] 性别色正确(男蓝 / 女粉)
  - [ ] 已故人物左上角斜线(贾代善 / 贾敏 / 林如海 / 贾珠)
- [ ] **嫡庶区分(还原历史真实)**:
  - [ ] 王夫人(principal)与赵姨娘(secondary)同代并存,均连贾政
  - [ ] 王夫人节点 160×60 + 实线;赵姨娘节点 140×50 + 实线但略小
  - [ ] 王夫人婚姻线 1.5px(粗);赵姨娘婚姻线 1px(细)
  - [ ] 贾环(庶子)节点 140×50 + 虚线边框,与贾宝玉(嫡子)160×60 + 实线边框 **明显视觉不同**
  - [ ] 同代兄弟排序:嫡子(元春/珠/宝玉/探春)在前,庶子(环)在后

### 顺序多次婚姻(`Family-Tree-SerialMarriage.md` — 亨利八世)

- [ ] 一人 6 次婚姻 layout 正确(亨利居中,6 配偶按 marriage_order 左右排开)
- [ ] divorced 婚姻线有 `//` 双斜线(凯瑟琳 / 安妮·克莱沃)
- [ ] 已故配偶有死亡斜线(简·西摩等)
- [ ] 各 Family 各自 drop-line + 子女(玛丽 / 伊丽莎白 / 爱德华各自挂母亲下)

### 同时多妻(`Family-Tree-Polygamy.md` — 康熙后宫)

- [ ] **同时多妻**(`concurrent_with` 互链)layout 正确,所有妃嫔同代一行
- [ ] 主妻(孝诚仁皇后 principal)紧贴康熙;妃嫔(secondary)外扩
- [ ] **混合**:前正妻已故 + 续正妻 + 同期纳妃 处理正确
- [ ] **嫡庶并存**:嫡子胤礽 + 庶子胤禛 + 庶子胤禩 同代视觉**明显**不同(嫡 160×60 实线,庶 140×50 虚线)

### 占位 + 单亲(`Family-Tree-Placeholders.md`)

- [ ] placeholder Person 渲染为虚线圆角矩形 + `?`
- [ ] 单 partner Family:不画婚姻横线,Family 锚点在 partner 正下方
- [ ] rel_type=adopted 的 ChildLink 是虚线 stub
- [ ] rel_type=unknown 的 ChildLink 也是虚线

### 通用回归

- [ ] 切换其他 variant(knowledge / basic)不受影响
- [ ] 通用 markdown(graph_variant: knowledge 等)导入仍正常
- [ ] 切换到 family-tree ViewMode 后再切回其他 ViewMode 不卡死 / 不残留
- [ ] typecheck 0 新错误
- [ ] 拖动节点时:配偶横线 + drop-line + sibling bar 即时跟随(走 GraphRenderer 的 updateConnectorsFor;v1 接受拖动期间 sibling bar 视觉略简化为直线兜底,松手后恢复正确)
- [ ] 拖动节点:配偶横线 / 亲子折线即时跟随(走 GraphRenderer 的 updateConnectorsFor)

## 12. 参考资料

- [GEDCOM 5.5.1 spec](https://en.wikipedia.org/wiki/GEDCOM)
- [GRAMPS 源码 — pedigreeview.py](https://github.com/gramps-project/gramps/blob/main/gramps/plugins/view/pedigreeview.py)
- [entitree-flex](https://github.com/codeledge/entitree-flex)
- [GenoPro genogram rules](https://genopro.com/genogram/rules/)
- van der Ploeg, A.J., *"Drawing Non-Layered Tidy Trees in Linear Time"*, 2013
